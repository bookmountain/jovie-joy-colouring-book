import { describe, it } from "vitest";

// TODO(P2-T17): rewrite against the async BE-backed catalog (with mocked fetch).
// The static-fixture assertions were removed when data/products + data/collections
// moved to async loaders in P2-T5/T6.
describe.skip("catalog helpers (pending rewrite in T17)", () => {
  it("placeholder", () => {});
});
